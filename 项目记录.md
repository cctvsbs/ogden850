# Ogden 850 单词学习应用 — 项目记录

> 当前版本：v0.8.1  
> 创建时间：2026年5月  
> 最后更新：2026-06-08  
> 线上地址：https://ogden850.netlify.app  
> GitHub：https://github.com/cctvsbs/ogden850

---

## 一、项目概述

基于 Ogden 850 基础英语词表的单词学习应用，包含闪卡背词、词意选择、拼写训练、表达训练四种模式。使用 Basic English 原则（仅 850 个基础单词 + 18 个核心动词）设计教学内容。

### 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Vue 3 + Vite 8 |
| 样式 | TailwindCSS 4 |
| 状态 | Pinia |
| 本地数据库 | Dexie.js (IndexedDB) |
| 语音 | Web Speech API |
| AI | Agnes API (文本 / 图像) |
| 部署 | Netlify |

### 项目规模

| 指标 | 数值 |
|------|------|
| 代码文件 | 37 个 |
| 代码行数 | ~4280 行 |
| 词表数据 | 851 词 |
| 音节数据 | 847 条 (99.5%) |
| 动词时态 | 18 个核心动词 |

### 数据文件

| 文件 | 说明 |
|------|------|
| `src/data/ogden850.json` | 851 个单词，含音标、中文释义、例句、Ogden 原则解析 |
| `src/data/ogden850-phonetic.json` | 847 条音节划分/音标/重音数据 |
| `src/data/ogden850-verbs.json` | 18 个核心动词的时态变化表 |
| `src/data/abstractWords.js` | 抽象词列表（不适合生成 AI 配图） |
| `src/data/expressionTasks.js` | 表达训练题库 |
| `src/data/functionWords.js` | 功能词列表 |

---

## 二、版本历史

### v0.8.1（2026-06-08）

| 修复项 | 改动 |
|--------|------|
| 取消 AI 评分 | 表达模式回归关键词匹配评分（60%阈值），移除 agnes-scoring.js 依赖 |
| 选择题记录 | 选择题答错自动记录到 mistakesService，答对自动移除错题 |
| 题目去重 | `buildNonRepeatPool()` + `commitRecentUsage()`，每次新会话排除最近使用的单词，最多 10% 重叠 |
| 卡片翻转音效 | WordCard.vue 翻转加 `playCardFlipSound()` |
| 移动端优化 | 按钮 min-height 48px、touch-manipulation、safe-area-inset-bottom |
| AI 图片保持 | `loadImageForCurrent` 从 Map 缓存同步恢复、移除所有 `currentImageUrl=null` 清空逻辑、`restoreImageCache` 页面加载从 IndexedDB 恢复 Map |
| 动词时态表 | 新建 ogden850-verbs.json，WordCard 和闪卡背面展示动词时态变化 |

### v0.8.0（2026-06-07）

| 功能 | 说明 |
|------|------|
| AI 智能评分 | 表达模式接入 Agnes API 评分，降级为关键词匹配 |
| AI 配图 | 闪卡 AI 配图替代 Pexels，IndexedDB 缓存 24h |
| AI 例句生成 | Agnes API 动态生成例句，IndexedDB 缓存 30min |
| 数据库 v3 | 新增 imageCache + exampleCache 表 |

### v0.7.1

- 4 个组件引用 8 个 composables，消除重复代码

### v0.7.0

- 重构：composables 拆分 + 数据文件重组 + IndexedDB 持久化

### v0.6.0

- 移动端浮动导航球、拼写二合一输入、语音引擎终极优化

### v0.5.0

- 护眼绿主题、表达模式、选择题键盘操作

---

## 三、Composables 列表

| 文件 | 功能 |
|------|------|
| `useSpeech.js` | 语音引擎（Voice 选择、链式播放、超时兜底） |
| `useAudioFeedback.js` | 音效（导航、选择、翻转、打字） |
| `useSyllableRendering.js` | 音节着色渲染 |
| `useFavorites.js` | 收藏夹管理 |
| `useKeyboardShortcuts.js` | 全局快捷键 |
| `useWordNavigation.js` | 单词导航 |
| `useFrequencyStars.js` | 频率星级显示 |
| `useExampleTokens.js` | 例句 token 分割 |

## 四、数据库 Schema

```
Dexie: ogden850 (v3)

progress:     wordId (主键), stage, nextReview, updatedAt
stats:        key (主键), value
settings:     key (主键), value
mistakes:     ++id, wordId, mode, createdAt, userAnswer, correctAnswer
favorites:    wordId (主键), createdAt
imageCache:   key, updatedAt, value
exampleCache: key, updatedAt, value
```

## 五、Agnes API

| 项目 | 值 |
|------|------|
| Base URL | `https://apihub.agnes-ai.com/v1` |
| 文本模型 | agnes-2.0-flash (chat/completions) |
| 图像模型 | agnes-image-2.1-flash (images/generations) |
| 评分 Prompt | 评估中译英翻译质量（语法30% + 词汇合规40% + 语义30%） |
| 例句 Prompt | 生成双语例句 JSON（basic + standard） |

## 六、评分系统

| 模式 | 评分方式 |
|------|----------|
| 表达模式 | 关键词匹配（60% 阈值） |
| 选择题 | 对错判断 + 错题记录 |
| 拼写模式 | 精确匹配 |
| 闪卡模式 | Again / Hard / Good / Easy 四档评级 |

## 七、待开发功能

无（之前的挑战模式 + 排行榜需求已取消）

## 八、发布流程

```bash
git add .
git commit -m "v0.8.1: 取消AI评分、选择题记录、题目去重、卡片音效、移动端优化、AI图片保持、动词时态表"
git push   # Netlify 自动部署
```
